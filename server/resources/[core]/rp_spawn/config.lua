RPSpawnConfig = {
  saveIntervalMs = 30000,
  fadeOutMs = 500,
  fadeInMs = 500,
  safeZOffset = 1.0,
  selfReviveCommand = 'selfrevive',
  fallProtection = {
    enabled = true,
    checkIntervalMs = 350,
    cooldownMs = 2500,
    zones = {}
  }
}
